<!doctype html><html lang="id"><head><meta charset="utf-8"><style>body{font:9px DejaVu Sans,sans-serif;color:#243e31}h1{font-size:19px}table{width:100%;border-collapse:collapse}td,th{padding:7px;border-bottom:1px solid #e2e6df;text-align:left;overflow-wrap:break-word}th{background:#eef4e8}small{color:#809175}</style></head><body><h1>{{ $business['name'] ?? '' }} · {{ $title }}</h1><p>{{ $snapshot ? 'Posisi saat laporan dibuat' : 'Periode: '.($from ?? 'Awal').' — '.($to ?? 'Sekarang') }}</p><p>Dibuat {{ now()->format('d/m/Y H:i') }} · {{ $user->name }}</p><table><thead><tr>@foreach($columns as $column)<th>{{ $column }}</th>@endforeach</tr></thead><tbody>@foreach($rows as $row)<tr>@foreach($row as $value)<td>{{ $value }}</td>@endforeach</tr>@endforeach</tbody></table></body></html>
