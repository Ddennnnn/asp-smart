{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "npm --prefix frontEnd_asp_smart run build",
        "dev": "npm --prefix frontEnd_asp_smart run dev",
        "lint": "npm --prefix frontEnd_asp_smart run lint",
        "test:e2e": "npm --prefix frontEnd_asp_smart run test:e2e"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
